<nav class="navbar navbar-expand-lg bg-primary" data-bs-theme="dark">
    <div class="container">
        <a class="navbar-brand" href="{{ path('app_home') }}">
            <span style="color: #28a745;">safe</span>
            <span style="color: white;">serv</span>
        </a>

        <button type="button" 
                class="navbar-toggler" 
                data-bs-toggle="collapse" 
                data-bs-target="#navbarColor01" 
                aria-controls="navbarColor01" 
                aria-expanded="false" 
                aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarColor01">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link text-secondary-emphasis" 
                       href="/guide-hygiene-haccp-2021.pdf" 
                       target="_blank">
                        Documentation
                    </a>
                </li>
            </ul>

            {% if app.user %}
                <a href="{{ logout_path() }}"class="btn btn-outline-secondary btn-sm">
                    Déconnexion
                </a>
            {% else %}
                <div class="d-flex">
                    <a href="{{ path('app_login') }}" class="btn btn-outline-success btn-sm">
                        Connexion
                    </a>
                    <a href="{{ path('app_register') }}" class="btn btn-outline-success btn-sm ms-2">
                        Enregistrement
                    </a>
                </div>
            {% endif %}
               {% if is_granted('ROLE_USER') and not  is_granted ('ROLE_ADMIN') %}
                <div class="d-flex">
                    <a href="{{ path('app_temp') }}" class="btn btn-outline-warning btn-sm ms-2">
                        Temperatures
                    </a>
                </div>
                <div class="d-flex">
                    <a href="{{path('app_temp_menu')}}"class="btn btn-outline-warning btn-sm ms-2">
                        Menu-semaines
                    </a>
                </div>
            {% endif %}

            {% if is_granted('ROLE_ADMIN') %}
                <div class="d-flex">
                    <a href="{{ path('app_menu') }}" class="btn btn-outline-info btn-sm ms-2">
                        Menu
                    </a>
                </div>
                <div class="d-flex">
                    <a href="{{path('app_conform')}}" class="btn btn-outline-info btn-sm ms-2">
                    conformités
                    </a>
                </div>
            {% endif %}
        </div>
    </div>
</nav>